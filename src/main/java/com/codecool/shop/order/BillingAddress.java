package com.codecool.shop.order;

public class BillingAddress extends Address{
    public BillingAddress(String country, int zipcode, String city, String address) {
        super(country, zipcode, city, address);
    }
}
