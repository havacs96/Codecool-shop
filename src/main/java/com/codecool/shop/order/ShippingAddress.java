package com.codecool.shop.order;

public class ShippingAddress extends Address{
    public ShippingAddress(String country, int zipcode, String city, String address) {
        super(country, zipcode, city, address);
    }
}
